# Simple Relay GraphQL Playground

## Frontend

To start the `vite` & `relay` watchers, run the following from the repository root:

```sh
yarn client:start
yarn client:relay
```

## Backend

Run:

```sh
yarn server:start
```
